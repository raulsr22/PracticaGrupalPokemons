<Page 
    x:Class="ProyectoUWPenBlanco.SeleccionCombatePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProyectoUWPenBlanco"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>

        <!-- Estilo bonito para el botón -->
        <Style x:Key="BotonPrimario" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Width" Value="300"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Padding="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Título -->
            <TextBlock Grid.Row="0" Text="Selecciona los Pokémon para combatir"
                       FontSize="32" FontWeight="Bold"
                       TextAlignment="Center" HorizontalAlignment="Center" Margin="0,10"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Jugador 1 (con recuadro fijo) -->
                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="Jugador 1 (ataca a la derecha)" 
                   FontSize="20" FontWeight="SemiBold" Margin="0,0,0,10" 
                   HorizontalAlignment="Center"/>
                    <Border Width="400" Height="430" BorderBrush="Gray" BorderThickness="1" CornerRadius="10">
                        <GridView x:Name="gvJugador1"
                      ItemsSource="{x:Bind ListaJugador1}"
                      SelectionMode="Single"
                      IsItemClickEnabled="True"
                      ItemClick="SeleccionJugador1_Click"
                      Width="400"
                      Height="Auto">
                            <GridView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsWrapGrid Orientation="Horizontal"
                       MaximumRowsOrColumns="2"
                       HorizontalAlignment="Center"/>
                                </ItemsPanelTemplate>
                            </GridView.ItemsPanel>

                            <GridView.ItemTemplate>
                                <DataTemplate x:DataType="local:Pokemon">
                                    <StackPanel Margin="10" Width="150" HorizontalAlignment="Center">
                                        <Image Source="{x:Bind Imagen}" Width="120" Height="120"/>
                                        <TextBlock Text="{x:Bind Nombre}" FontSize="16" TextAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridView.ItemTemplate>
                        </GridView>
                    </Border>
                </StackPanel>

                <!-- Separador vertical -->
                <Rectangle Grid.Column="1" Width="1" Fill="Gray" Margin="20"/>

                <!-- Jugador 2 (mismo tamaño, scroll interno) -->
                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="Jugador 2 (ataca a la izquierda)" 
                   FontSize="20" FontWeight="SemiBold" Margin="0,0,0,10" 
                   HorizontalAlignment="Center"/>
                    <Border Width="400" Height="430" BorderBrush="Gray" BorderThickness="1" CornerRadius="10">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <GridView x:Name="gvJugador2"
                          ItemsSource="{x:Bind ListaJugador2}"
                          SelectionMode="Single"
                          IsItemClickEnabled="True"
                          ItemClick="SeleccionJugador2_Click"
                          Width="400"
                          Height="Auto">
                                <GridView.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <ItemsWrapGrid Orientation="Horizontal"
                       MaximumRowsOrColumns="2"
                       HorizontalAlignment="Center"/>
                                    </ItemsPanelTemplate>
                                </GridView.ItemsPanel>

                                <GridView.ItemTemplate>
                                    <DataTemplate x:DataType="local:Pokemon">
                                        <StackPanel Margin="10" Width="150" HorizontalAlignment="Center">
                                            <Image Source="{x:Bind Imagen}" Width="120" Height="120"/>
                                            <TextBlock Text="{x:Bind Nombre}" FontSize="16" TextAlignment="Center"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                            </GridView>
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </Grid>



            <!-- Botón inferior bonito -->
            <Button Grid.Row="2" x:Name="btnComenzar"
                    Style="{StaticResource BotonPrimario}"
                    Content="Comenzar combate"
                    Click="btnComenzar_Click"
                    Margin="0,20"
                    IsEnabled="False"/>
        </Grid>
    </ScrollViewer>
</Page>
